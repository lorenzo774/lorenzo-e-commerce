extends layout 

block content 
    .d-flex
        if product.image 
            img.w-50.p-4(src=product.image)

        .p-4
            h1= product.name 
            h3 #{product.description}

            h3 #{product.price} €
            h3 Category: #{product.category.name}