extends layout 

block content 
    p.display-3= title 
    if orders.length
        ul
        each order in orders 
            li 
                a(href=order.url) Order of #{order.localeDate}
    else 
        h3 No Orders